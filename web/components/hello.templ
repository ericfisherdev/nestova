package components

// Hello renders a minimal greeting. It exists to verify the templ-to-Go
// generation pipeline and the runtime render path; feature components replace
// it as the UI is built out.
templ Hello(name string) {
	<h1>Hello, { name }!</h1>
}
