// Package adapter holds inbound (HTTP/templ handlers) and outbound (persistence) adapters
// for the meals bounded context (meal planning, the recipe box, and the ingredient-driven recipe finder).
//
// This package is part of the project's hexagonal/DDD layout and is
// populated as the meals features land.
package adapter
