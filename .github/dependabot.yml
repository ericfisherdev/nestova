version: 2
updates:
  # Keep the SHA-pinned GitHub Actions in ci.yml current (Dependabot bumps both
  # the pinned commit and its version comment).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
